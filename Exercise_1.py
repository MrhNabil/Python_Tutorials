# Exercise 1 rectangle area calculation

length = float(input("Enter the length: "))
weight = float(input("Enter the weight "))
area = length * weight # this will show error until we typecast the length and weight in int or float

print(f"The are is {area} cm²") # num_lock + Alt + 0178 = ² 